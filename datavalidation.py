import random
import string


def collect_details():
    first_name = input('Enter your first name: ')
    last_name = input('Enter your last name: ')
    email = input('Email address: ')
    details = (first_name, last_name, email)
    return details

def generate_password(details):
    characters = string_ascii_letters
    length = 9
    password =
    generate_password()

    password = string_ascii_letters
